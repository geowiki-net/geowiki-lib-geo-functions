# geowiki-lib-geo-functions
Various geometry functions which are used by several modules.

## FUNCTIONS
* parseLength(value, metersPerPixel) - parses a value (e.g. '4m') to view pixels at the current zoom level.
